# weights and captures — never in git
*.safetensors
*.ckpt
*.pt
*.pth
*.mp4
*.wav
# ...except the two clips the README embeds. They are the only evidence a
# first-time reader has that any of this works, and a link to a file that is
# not in the repository is worse than no link.
!docs/media/*.mp4

# build / env
.build/
.swiftpm/
.venv/
.DS_Store

# secrets — belt and braces
.env
*.token
**/hf_token*

# release archives
dist/
